{{define "layout"}}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="UTF-8">
    <title>{{if .Title}}{{.Title}}{{else}}Secrets{{end}}</title>
    <style>
        {{template "main.css" .}}
    </style>
</head>
<body>
    {{template "content" .}}
</body>
</html>
{{end}}
